import {
  INTRO_LABEL,
  RECAP_LABEL,
} from './button-labels';

export const SKIP_INTRO_SELECTOR = `[aria-label="${INTRO_LABEL}"]`;
export const SKIP_RECAP_SELECTOR = `[aria-label="${RECAP_LABEL}"]`;
